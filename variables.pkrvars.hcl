instance_type = "t2.micro"
ssh_username = "admin"
ami_users = ["default ownerID and the ownerID of the user that you want to share the AMI with"] //for example create the AMI in dev and share it with prod
aws_profile = "aws-cli-ami"
